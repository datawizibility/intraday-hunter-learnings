{
  "name": "intraday-hunter-learnings-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-vault.mjs",
    "predev": "npm run sync",
    "dev": "vite",
    "prebuild": "npm run sync",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "marked": "^15.0.12",
    "vite": "^6.3.5"
  }
}
